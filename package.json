{
  "name": "rems",
  "workspaces": [
    "apps/app",
    "apps/ui",
    "apps/cms",
    "packages/types"
  ],
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "postinstall": "if [[ $NODE_ENV == \"production\" ]]; then cd apps/cms && npm run build; fi",
    "start": "cd apps/cms && npm run start"
  }
}
