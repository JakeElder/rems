import React from "react";
import css from "./ViewFilters.module.css"

type Props = {};

const ViewFilters = ({}: Props) => {
  return <div className={css["root"]}>ViewFilters</div>;
};

export default ViewFilters;
