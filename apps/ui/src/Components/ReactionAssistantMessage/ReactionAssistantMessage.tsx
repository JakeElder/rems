import React from "react";
import css from "./ReactionAssistantMessage.module.css"

type Props = {};

const ReactionAssistantMessage = ({}: Props) => {
  return <div className={css["root"]}>ReactionAssistantMessage</div>;
};

export default ReactionAssistantMessage;
